class Pet(object): # Pet is a class of the type `object` 
    def __init__(self, name, species): # Class initialization: _init__ is method (special Python function) is called when a instance of a class is first created.
    shelf.name = name
    shelf.species = species